opencv-python>=4.10.0,<5.0.0
mediapipe>=0.10.18,<0.11.0
pyautogui>=0.9.54,<1.0.0
numpy>=1.26.0,<2.2.0
pytest>=8.0.0


# Opcional: backend ModernGL 3D pro holograma (v6.9.8+).
# HOLOGRAM_BACKEND="auto" no config tenta GL e cai pra QPainter se nao
# disponivel — projeto funciona normalmente sem moderngl instalado.
moderngl>=5.10.0

# Hook global de tecla — permite toggle do Smart Keyboard (K) com foco em
# QUALQUER janela do SO, nao so na janela OpenCV. Service soft-fail se
# ausente (cai pra cv2.pollKey original).
keyboard>=0.13.5
